예제 16>
a<-subset(iris,Species==“setosa” | Species==“vesicolor”)
a1<-factor(a$Species)
b<-glm(Species〜Sepal.Length,data=a,family = binomial)
summary(b)

coef(b)

fitted(b)[c(1:3,98:100)]
predict(b,newdata = a[c(1,50,51,100),],type = "response”)

